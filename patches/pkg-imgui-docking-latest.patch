From a8df192df022ed6ac447e7b7ada718c4c4824b41 Thu Nov 24 21:24:00 2022
From: MarcasRealAccount <thetremine.lumicks@gmail.com>
Date: Fri, 16 Dec 2022 17:54:00 +0100
Subject: [PATCH] Add premake scripts :>

---
 premake5.lua | 40 ++++++++++++++++++++++++++++++++++++++++
 build.lua    | 29 +++++++++++++++++++++++++++++
 dep.lua      | 3  +++
 2 files changed, 42 insertions(+)
 create mode 100644 premake5.lua
 create mode 100644 build.lua
 create mode 100644 dep.lua

diff --git a/premake5.lua b/premake5.lua
new file mode 100644
index 0000000..67fbaad
--- /dev/null
+++ b/premake5.lua
@@ -0,0 +1,40 @@
+workspace("imgui")
+	location("build/")
+	configurations({ "Debug", "Release", "Dist" })
+	platforms({ "amd64" })
+	filter("platforms:amd64")
+		architecture("amd64")
+	filter("configurations:Debug")
+		optimize("Off")
+		symbols("On")
+	filter("configurations:Release")
+		optimize("Full")
+		symbols("On")
+	filter("configurations:Dist")
+		optimize("Full")
+		symbols("Off")
+	filter({})
+	cppdialect("C++20")
+	rtti("Off")
+	exceptionhandling("Off")
+	flags("MultiProcessorCompile")
+	project("imgui")
+		location("%{wks.location}/")
+		kind("StaticLib")
+		targetdir("%{wks.location}/imgui/%{cfg.buildcfg}")
+		objdir("%{wks.location}/imgui/%{cfg.buildcfg}")
+		includedirs({ "%{wks.location}" })
+		files({
+			"imgui.cpp",
+			"imgui_draw.cpp",
+			"imgui_tables.cpp",
+			"imgui_widgets.cpp"
+		})
+
+	project("imgui_demo")
+		location("%{wks.location}/")
+		kind("StaticLib")
+		targetdir("%{wks.location}/imgui_demo/%{cfg.buildcfg}")
+		objdir("%{wks.location}/imgui_demo/%{cfg.buildcfg}")
+		includedirs({ "%{wks.location}" })
+		files({ "imgui_demo.cpp" })
\ No newline at end of file
diff --git a/build.lua b/build.lua
new file mode 100644
index 0000000..49accfb
--- /dev/null
+++ b/build.lua
@@ -0,0 +1,29 @@
+local pkg       = premake.extensions.pkg
+local scriptDir = common:scriptDir()
+
+local buildTool = pkg:setupPremake("imgui", { "Debug", "Release", "Dist" }, scriptDir, "build/")
+buildTool:mapConfigs({
+	Debug = {
+		config  = "Debug",
+		targets = {
+			imgui      = { path = "imgui/", outputFiles = common:libName("imgui", true) },
+			imgui_demo = { path = "imgui_demo/", outputFiles = common:libName("imgui_demo", true) }
+		}
+	},
+	Release = {
+		config  = "Release",
+		targets = {
+			imgui      = { path = "imgui/", outputFiles = common:libName("imgui", true) },
+			imgui_demo = { path = "imgui_demo/", outputFiles = common:libName("imgui_demo", true) }
+		}
+	},
+	Dist = {
+		config  = "Dist",
+		targets = {
+			imgui      = { path = "imgui/", outputFiles = common:libName("imgui", false) },
+			imgui_demo = { path = "imgui_demo/", outputFiles = common:libName("imgui_demo", false) }
+		}
+	}
+})
+buildTool:build()
+buildTool:cleanTemp()
\ No newline at end of file
diff --git a/dep.lua b/dep.lua
new file mode 100644
index 0000000..7cc6bb6
--- /dev/null
+++ b/dep.lua
@@ -0,0 +1,3 @@
+libdirs({ common:scriptDir() .. string.format("/Bin/%s-%s-", common.host, common.arch) .. "%{cfg.buildcfg}" })
+links({ "imgui", "imgui_demo" })
+externalincludedirs({ common:scriptDir() })
\ No newline at end of file
--
2.38.1.windows.1